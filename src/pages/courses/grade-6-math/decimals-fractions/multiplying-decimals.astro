---
import Unit4DecimalMultiplyLessonPage from '../../../../components/Unit4DecimalMultiplyLessonPage.astro';
import { findCourse } from '../../../../data/courses';
import { unit4Lesson45QAContent } from '../../../../data/unit4Lesson45Enhancements';

const course = findCourse('grade-6-math')!;
const unit = course.units.find((item) => item.slug === 'decimals-fractions')!;
const topic = unit.topics.find((item) => item.slug === 'multiplying-decimals')!;
const content = unit4Lesson45QAContent;
---
<Unit4DecimalMultiplyLessonPage {course} {unit} {topic} {content} />
