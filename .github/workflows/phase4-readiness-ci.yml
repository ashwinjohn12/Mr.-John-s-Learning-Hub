name: Phase 4 classroom readiness validation

on:
  push:
    branches:
      - audit/phase4-classroom-readiness-20260913

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: withastro/action@v3
